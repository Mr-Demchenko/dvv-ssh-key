[project]
name = "oop"
version = "0.1.0"
description = ""
authors = [
    {name = "Mr.Vit",email = "mr.white.vw@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest (>=9.1.1,<10.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
flake8 = "^7.3.0"
pytest-cov = "^7.1.0"

